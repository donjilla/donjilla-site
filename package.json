{
  "name": "donjilla-site",
  "version": "1.0.0",
  "description": "donjilla.com — photography portfolio and archive directory",
  "scripts": {
    "build": "npx @11ty/eleventy",
    "start": "npx @11ty/eleventy --serve --watch"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1"
  }
}
